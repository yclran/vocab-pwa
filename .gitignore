# 依赖
node_modules

# 构建产物（由 CI / 本地 build 生成）
dist
dist-prod
dist-new

# 环境变量（含密钥，切勿提交）
.env
.env.local
.env.*.local

# 系统 / 编辑器
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
.idea

# 临时文件
_tmp
~

# WorkBuddy 内部状态（非项目代码，无需提交）
.workbuddy/
